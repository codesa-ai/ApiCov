name: Release Action

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write  # Required for creating releases
  packages: write  # Required for publishing packages

jobs:
  build:
    runs-on: ubuntu-22.04  # Always build on Ubuntu 22.04 for maximum compatibility

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Current commit
        run: |
          echo "Checked out: $(git rev-parse HEAD)"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Create virtual environment
        run: |
          uv venv .venv

      - name: Install dependencies
        run: |
          source .venv/bin/activate
          pip install -e .
          pip install beautifulsoup4 lxml

      - name: Confirm dependencies were installed
        run: |
          pip show beautifulsoup4
          pip show lxml

      - name: Build binary
        id: build
        run: |
          source .venv/bin/activate
          echo "✅ Checking current directory and files"
          pwd
          ls -R

          echo "✅ Cleaning previous builds"
          rm -rf build/ dist/ __pycache__ .cache/pyinstaller
      
          echo "✅ Running PyInstaller"          
          pyinstaller --onefile \
            --clean \
            --strip \
            --noconfirm \
            --add-binary "/usr/lib/x86_64-linux-gnu/libm.so.6:." \
            --hidden-import=modules \
            --hidden-import=requests \
            --hidden-import=bs4 \
            --hidden-import=bs4.builder._lxml \
            --hidden-import=lxml \
            --hidden-import=lxml.etree \
            --hidden-import=xml \
            --hidden-import=xml.etree \
            --hidden-import=xml.etree.ElementTree \
            --hidden-import=xml.parsers \
            --hidden-import=xml.parsers.expat \
            --collect-submodules bs4 \
            --collect-submodules lxml \
            --collect-submodules xml \
            --collect-data bs4 \
            --collect-data lxml \
            --add-data "src/modules:modules" \
            src/apicov.py
    
          # Ensure binary is executable
          chmod +x dist/apicov
          
          # Save binary location
          echo "binary_path=$(pwd)/dist/apicov" >> $GITHUB_OUTPUT

      - name: List build files
        run: |
          echo "Listing files in dist directory:"
          ls -la dist/

      - name: Update binary in tag
        run: |
          echo "Current working directory: $(pwd)"
          echo "Listing current directory contents:"
          ls -la
          
          # Configure git
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Get the current tag
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Current tag: $TAG_NAME"
          chmod +x dist/apicov
          
          # Commit the binary (creates new commit in detached HEAD)
          # Use -f as dist/* is in our .ignore
          git add -f dist/apicov
          echo "Committing changes"
          git commit -m "Add binary for $TAG_NAME [skip ci]"
          
          # Force-move tag to the new commit
          echo "Moving tag to a new commit"
          git tag -f $TAG_NAME
          git push origin $TAG_NAME --force

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: apicov-binary
          path: |
            dist/apicov

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Current commit
        run: |
          echo "Checked out: $(git rev-parse HEAD)"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: |
          echo "Listing downloaded artifacts:"
          find artifacts -type f

      - name: Prepare release files
        run: |
          # Create release directory
          mkdir -p release
          
          # Copy action.yml from the repository
          cp action.yml release/action.yml

          # Copy binary and script to release directory
          mkdir -p "release/dist"
          cp "artifacts/apicov-binary/apicov" "release/dist/apicov"
          
          # Ensure files are executable
          chmod +x "release/dist/apicov"

      - name: Commit dist to repository
        run: |
          # Configure git
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          # Get the current tag
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Current tag: $TAG_NAME"
          
          # Copy binary to dist/ in repo root
          mkdir -p dist
          cp release/dist/apicov dist/apicov
          chmod +x dist/apicov
          
          # Add and commit
          git add dist/apicov
          git commit -m "Update dist for $TAG_NAME [skip ci]" || echo "No changes to commit"
          
          # Push to branch
          BRANCH_NAME=$(git branch --show-current)
          echo $BRANCH_NAME
          #git push origin HEAD:main

      - name: List release files and save paths
        id: list_files
        run: |
          echo "Listing files in release directory:"
          find "$GITHUB_WORKSPACE/release" -type f > file_paths.txt
          cat file_paths.txt
          echo "files<<EOF" >> $GITHUB_OUTPUT
          while IFS= read -r file; do
            echo "$file" >> $GITHUB_OUTPUT
          done < file_paths.txt
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ steps.list_files.outputs.files }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}